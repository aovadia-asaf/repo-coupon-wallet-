PORT=3001
FAMILY_PIN=1234
SESSION_SECRET=change-me-to-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173
DATABASE_URL="file:./dev.db"

# Production only (e.g. Railway) — leave unset for local dev:
# NODE_ENV=production
# DATABASE_URL="file:/data/prod.db"   # path inside a mounted persistent volume
# UPLOAD_DIR=/data/uploads            # path inside a mounted persistent volume
